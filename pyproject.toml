[project]
name = "simethicone"
version = "0.1.0"
description = "End-to-end ETL + API to find the most fuel-efficient vehicles in a given region."
readme = "README.md"
requires-python = "==3.11"
dependencies = []
